# ashp dashboard

A small [Streamlit](https://streamlit.io/) + [Plotly](https://plotly.com/) app
for exploring alpha shapes interactively.

## Run

From the repository root:

```bash
uv run --group dashboard streamlit run apps/dashboard/app.py
```

The `--group dashboard` flag is **required** — it installs Streamlit/Plotly into
the project environment. Without it, `uv run streamlit ...` finds no Streamlit in
the venv and falls back to a system/nix-provided one that cannot import `ashp`
(`ModuleNotFoundError: No module named 'ashp'`). Running `uv sync --group
dashboard` once also works, after which a plain `uv run streamlit ...` is fine.

Pick a dataset — 2-D (two moons, spiral, annulus, uniform) or 3-D (ball, torus,
blobs) — set the number of points, and choose how alpha is selected:

- **Quantile (robust)** — `select_alpha(points, q)`; a rank statistic, so the
  shape stays stable as you change the point count (the default).
- **Manual** — set alpha directly (its useful range shifts with point count).
- **Optimize (cover all)** — `optimizealpha`; tightest single polygon covering
  every point (2-D only).

3-D datasets render the alpha-shape surface mesh with Plotly.

The **Clustering** tab treats the connected components of the alpha complex as
clusters (the single-linkage / DBSCAN-like view) and shows a persistence curve —
cluster count vs alpha — with the most stable scale picked automatically, read
like a k-means elbow plot. Points are coloured by cluster (noise in grey).

## Files

| File | Purpose |
|------|---------|
| `app.py` | the Streamlit dashboard |
| `plotting.py` | shared point-sampling + Plotly helpers (also used by the image generator) |

The gallery images in the README are generated by `assets/generate_images.py`,
which reuses `plotting.py` from here.

## Regenerate gallery images

```bash
uv run --group dashboard python assets/generate_images.py   # -> assets/img/
```

> **Note:** the 2-D reconstruction unions boundary polygons, so interior holes
> (e.g. the centre of the *annulus* dataset) are filled rather than carved out.
